# image_region_area_statistics
Given an aerial image, we need to count the size of different regions (regions with similar colors are considered to be the same continuous region). Input an image and output the number of continuous regions in the image and the size of each continuous region.
